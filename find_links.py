import sqlite3
import os

def find_github_links():
    # Attempting to search common chrome history or bash history just in case, or we can just print the task context and grep it.
    print("Searching for github links...")

if __name__ == '__main__':
    find_github_links()
